# 外交部国家和组织离线信息库

这是一个本地 `Windows` 桌面程序，用于整理和离线检索外交部官网公开页面中的国家、地区和国际组织简介信息。

这个公开仓库只提供源码，不附带现成数据库快照，也不提供打包后的可执行文件。

## 公开发布说明

- 本项目是个人开发工具，`不是官方产品`。
- 仓库不包含抓取后的数据库，也不默认分发内置数据的 `exe`。
- 数据来源于外交部官网公开页面，具体内容、表述和更新情况以原站为准。
- 本项目主要用于学习、检索和信息整理，请在实际使用前自行评估数据合规性和适用场景。

## 当前功能

- 本地 SQLite 数据库存储
- 名称优先的离线检索与筛选
- “结构化信息 + 正文”并排阅读
- HTML 正文渲染和表格显示
- 世界地图独立窗口，点击国家后跳转正文
- 首次无数据时可在程序内执行同步

## 首次使用

1. 运行程序。
2. 首次启动会自动释放地图资源到：
   `%LOCALAPPDATA%\\MfaLocalDb`
3. 如果本地数据库为空，点击界面上的 `同步官网` 按钮，等待程序抓取并建立本地数据库。

## 开发运行

```powershell
dotnet run --project .\MfaLocalDb\
```

## 发布本地 exe

如果你只是给自己或受控范围内的同事使用，可以在本地自行构建：

```powershell
dotnet publish .\MfaLocalDb\ -c Release
```

发布产物位于：

`MfaLocalDb\bin\Release\net8.0-windows\win-x64\publish\MfaLocalDb.exe`

## 建议的公开分享方式

- 分享源码仓库、截图和使用说明
- 不公开上传内置数据库的发布包
- 如需公开发布，请补充你自己的 `LICENSE` 决策和更完整的数据来源说明
